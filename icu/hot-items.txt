# Compress ONLY the five display-name trees (curr/ lang/ region/ unit/ zone/).
# Everything else stays raw so Collator, Segmenter, DateTimeFormat,
# NumberFormat (default), Date, URL, normalize, regex \p{} pay zero
# decompression in any locale.
#
# Within the trees, only */pool.res stays raw: it's a shared string table
# (85-191 KB per tree) that every locale bundle references into, so
# compressing it would add ~100-150 us to the first call of each tree's API
# regardless of locale. Everything else (root, en, en_*, supplemental,
# tzdbNames, and all ~500 locale bundles) is compressed.

*.icu
*.nrm
*.res
brkitr/*
coll/*
*/pool.res
