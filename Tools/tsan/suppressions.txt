# ThreadSanitizer suppressions for the JSC TSAN no-JIT build (tsan.sh).
#
# Pass at runtime via:
#   TSAN_OPTIONS="suppressions=$PWD/Tools/tsan/suppressions.txt" WebKitBuild/TSan/bin/jsc ...
#
# RULES — read before adding anything:
#   1. Only KNOWN-BENIGN, PRE-EXISTING races may be suppressed here, i.e.
#      races that exist in the upstream/serial JSC tree independent of the
#      shared-memory Thread work (e.g. intentionally racy profiling
#      counters that concurrent compiler threads already read).
#   2. Every entry MUST carry a one-line justification comment directly
#      above it explaining why the race is benign and where it predates
#      the threads work (file:line or upstream reference).
#   3. NEVER suppress a race in code touched by the threads workstreams
#      (object model, heap handout, atom table, watchpoints, VM-lite).
#      Those are bugs; fix them instead.
#   4. Prefer the narrowest matcher available: `race:Function` over
#      `race:file.cpp` over `race:module`. No blanket suppressions.
#
# Syntax reference:
#   race:FunctionNameSubstring
#   race:path/substring.cpp
#   deadlock:FunctionNameSubstring
#
# STATUS: contains only the six pre-existing upstream parallel-GC entries
# at the bottom of this file (adjudicated under rule 1; see each entry's
# justification). Historical note (Verify phase): the file started empty —
# TSAN runs of hello-world and the single-layer corpus files
# (lock-hold-mutual-exclusion, property-read-write, atomics-object-basic,
# condition-wait-notify) under WebKitBuild/TSan/bin/jsc reported ZERO data
# races. The only other TSAN noise ever seen was:
#   - "thread leak" on every Thread() spawn -- fixed in source
#     (constructThread now detaches the native handle, ThreadObject.cpp),
#     not suppressed;
#   - an intermittent SEGV in CLoop::execute -- the shared-CLoopStack
#     limitation of the phase-1 GIL stub (documented in
#     docs/threads/TSAN.md "Known limitations"), not a data race.

# Pre-existing upstream parallel/concurrent marking races, surfaced once
# GC-heavy corpus tests (heap-bench-allocation.js, heap-option-off.js) run
# parallel marking helpers under the TSAN no-JIT build. heap-option-off.js
# reproduces with the shared-heap option OFF, i.e. pure upstream behavior.
# The threads work made comment-only changes at these sites; none of them is
# threads-workstream code (object model, heap handout, atom table,
# watchpoints, VM-lite).

# Benign: upstream publishes header().m_markingVersion after an explicit
# WTF::storeStoreFence() (Source/JavaScriptCore/heap/MarkedBlock.cpp:394-395);
# other markers read the version racily by design (MarkedBlock::areMarksStale).
race:JSC::MarkedBlock::aboutToMarkSlow

# Benign: upstream's deliberately lock-free MarkStackArray::size() heuristic
# reads ("Avoid locking..." / "be conservative",
# Source/JavaScriptCore/heap/SlotVisitor.cpp:437-444) race with
# GCSegmentedArray<T>::postIncTop (heap/GCSegmentedArrayInlines.h:142) from
# MarkStackArray::donateSomeCellsTo; the actual donation runs under
# m_heap.m_markingMutex. Covers both stacks of the size-vs-postIncTop report.
race:JSC::SlotVisitor::donateKnownParallel

# Benign: ParallelHelperClient::claimTask increments m_numActive under the
# pool lock (Source/WTF/wtf/ParallelHelperPool.cpp:104) while runTask's
# RELEASE_ASSERT(m_numActive) (:110) reads it unlocked; the asserting helper
# itself incremented the counter under the lock, so nonzero is guaranteed.
# Upstream WTF code untouched by the threads work.
race:WTF::ParallelHelperClient::claimTask

# Round-3 additions: next-layer upstream parallel-marking heuristics that were
# masked behind the three entries above under halt_on_error=1. noteMarked
# reproduces with the shared-heap option OFF (heap-option-off.js), i.e. pure
# upstream behavior; the two Heap.cpp sites are adjudicated by code inspection
# (upstream lock-free CAS loop and upstream sampling cache). None of these
# three function bodies is touched by the threads workstreams.

# Benign: upstream's own comment at the racy increment says "This is racy by
# design. We don't want to pay the price of an atomic increment!"
# (Source/JavaScriptCore/heap/MarkedBlockInlines.h:571-577); m_biasedMarkCount
# is a heuristic where lost updates are tolerated by design.
race:JSC::MarkedBlock::noteMarked

# Benign: the flagged frame is the plain seed load `size_t oldSize = *counter;`
# of upstream's lock-free CAS retry loop
# (Source/JavaScriptCore/heap/Heap.cpp:3340-3355); a stale read fails the
# atomicCompareExchangeWeakRelaxed and retries, so the extra-memory total
# stays consistent. Upstream-intentional lock-free accounting.
race:JSC::Heap::reportExtraMemoryVisited

# Benign: upstream sampling cache — non-atomic
# ++m_percentAvailableMemoryCachedCallCount and unsynchronized read of the
# cached bool m_overCriticalMemoryThreshold
# (Source/JavaScriptCore/heap/Heap.cpp:765-774); lost increments/stale reads
# only delay a refresh of an approximate memory-pressure heuristic.
race:JSC::Heap::overCriticalMemoryThreshold

# ============================================================================
# V7 adjudication record (tsan-sharedgc-marking-races, 202-report corpus).
# NO new suppressions added in this round — comments only. Reviewed 3/3
# approve with binding amendments; this block records the state.
#
# Part A — five plain-data-race families FIXED IN CODE (not suppressed):
#   1. IsoCellSet parallel source Task::m_done -> Atomic<bool> relaxed
#      (heap/IsoCellSet.cpp; fully converted, class is .cpp-local).
#   2. MarkingConstraint::m_lastVisitCount -> WTF::atomicStore/ExchangeAdd
#      relaxed at all four .cpp accesses; doParallelWork's m_lock critical
#      section elided (sole user).
#   3. SlotVisitor::m_mutatorIsStopped -> WTF::atomicStore/atomicLoad relaxed
#      at SlotVisitor.cpp:469/481/486 (also fixed bitwise & on bools -> &&).
#   4. Heap::m_worldIsStopped -> release stores at Heap.cpp stopThePeriphery/
#      resumeThePeriphery, relaxed self-check loads.
#   5. Thread::m_gcThreadType bit-field RMW (Threading.cpp:410) — NOT yet
#      fixed; requires Threading.h layout change (move out of the packed
#      bit-field byte shared with m_isShuttingDown/m_didExit/
#      m_isCompilationThread/m_isJSThread, make Atomic<uint8_t>). FIXME left
#      at the site; MUST land in the header-side pass.
#   RESIDUAL (header-side pass required, headers were out of scope for this
#   change): plain reads remain at MarkingConstraint.h:63 lastVisitCount(),
#   SlotVisitor.h:166 mutatorIsStopped(), Heap.h:414 worldIsStopped(),
#   Threading.h:289 gcThreadType(). TSAN may still pair these header-inlined
#   reads against the now-atomic .cpp stores; those reports mean "finish the
#   header pass", not "suppress".
#   Adjudication caveats carried from review: these five sites are upstream
#   PRE-EXISTING races newly OBSERVED (not newly created) by the shared-GC
#   election; the relaxed-atomic conversion permanently blinds TSAN at these
#   sites — re-audit them if the N-mutator stop protocol grows a real
#   ordering dependency. Ledger note: proposal counted 176 Part A / 26
#   Part B but its enumerations sum 177/25; reconcile to exactly 202 against
#   a FRESH TSan run before any Part B entry lands.
#
# Part B — remaining ~25 reports (upstream lock-free mark-bit protocol:
# wtf/Atomics.h:55/:81 via concurrentTestAndSet, IsoCellSetInlines.h:45,
# BlockDirectoryBits.h:98 raw word() views, FastBitVector FastBitReference
# operator=, MarkedBlockInlines.h:265 specializedSweep, SlotVisitor.cpp:330
# noteLiveAuxiliaryCell, :299 appendToMarkStack): DO NOT SUPPRESS YET.
# Binding amendment replaces the proposal's control: rebuild WebKitBuild/TSan
# at the current tree, then run the same tests in STOCK-UPSTREAM config — NO
# thread flags at all (not GIL-off-minus-shared-heap) — with
# --numberOfGCMarkers=8 and forced full GCs. Only signatures reproducing in
# that stock run qualify under rule 1; everything else escalates as a
# possible real shared-GC bug. Candidate entries (complete, untruncated
# list; each still needs its own rule-2 justification line, and prefer
# narrower anchors, e.g. concurrentTestAndSet over IsoCellSet::add, if
# TSAN frame matching allows):
#   race:JSC::IsoCellSet::add
#   race:JSC::MarkedBlock::Handle::specializedSweep
#   race:JSC::SlotVisitor::noteLiveAuxiliaryCell
#   race:JSC::SlotVisitor::appendToMarkStack
#
# Mandatory post-apply gates (next verify round): V5a/V5b bench re-run
# (flag-off codegen changed: GC-path atomic RMWs in shared upstream code),
# GIL-on corpus (92 tests), pinned V2/V3/V4 rungs.
# ============================================================================
